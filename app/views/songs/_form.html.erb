<%= form_for @song, url: form_url_helper(@artist)  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.label :artist_name %><br>

  <%= display_name(@song, @path) %>
  <%= artist_select(@song, @path) %><br>

  <%= f.submit %>
<% end %>
